#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <unistd.h>
#include <sys/socket.h>
#include <arpa/inet.h>

int main(int argc, char **argv)
{
    /* 创建套接字 */

    /* 设置端口重用 */

    /* 绑定IP地址 */

    /* 设置监听 */

    /* 链接 */

    /* 设置缓冲区大小 */

    /* 设置缓冲区下限 */

    /* 设置超时接受时间 */

    while(1)
    {
        
    }




}